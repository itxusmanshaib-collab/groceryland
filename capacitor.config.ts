import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'groceryland',
  webDir: 'dist/groceryland/browser'
};

export default config;
